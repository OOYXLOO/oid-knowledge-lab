# Local Video Notes

This folder contains the reproducible HyperFrames composition for the DataHub
Context Compass demo video. Generated renders belong in `renders/` and can be
committed when they are final submission assets.

Do not add private account screenshots, credentials, tokens, cookies, or real
customer catalog data.
