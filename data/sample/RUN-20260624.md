# Sample Crawl Receipt - 2026-06-24

This receipt records a small parser-validation crawl. It does not include copied OID page content.

## Source Checks

- `robots.txt`: fetched successfully.
- `sitemap.xml`: fetched successfully.
- Sitemap URL count: `7497`
- OID URL count in sitemap: `7492`
- Allowed by robots: `/sitemap.xml`, `/get/0`, `/get-md/0`
- Disallowed by robots and not crawled: `/cgi-bin/display?tree=`

## Command

```bash
npm run crawl:sample
npm run report
```

Earlier resume checks on 2026-06-24 also verified that interrupted crawls can append pending entries without duplicating completed OIDs:

```bash
node src/cli.js crawl --limit 1 --delay-ms 500 --out data/sample --resume
node src/cli.js report --in data/sample/records.jsonl --out data/sample/report.json
node src/cli.js crawl --limit 3 --delay-ms 500 --out data/sample --resume
node src/cli.js report --in data/sample/records.jsonl --out data/sample/report.json
```

The crawler was re-run in resume mode after the publishable package audit:

```bash
npm run crawl:sample:resume
npm run report
```

The crawler was re-run again in resume mode after the 2026-06-24 18:41 +08 publishable refresh:

```bash
npm run crawl:sample:resume
npm run report
```

## Result

- Latest full sample run: `25` parsed OID Markdown records.
- Latest resume run: `25` additional parsed OID Markdown records.
- Latest run timestamp: `2026-06-24T10:42:42Z`.
- Completed before latest resume run: `50`
- Completed after latest resume run: `75`
- Cumulative local parser-validation records: `75`
- Delay: `1000 ms`
- Resume enabled for latest run: `true`
- Raw Markdown saved: `false`
- Full collection authorized: `false`
- First OID in latest resume run: `1.3.6.1.4.1.65995`
- Last OID in cumulative sample: `1.3.6.1.4.1.66019`
- Root arcs in cumulative sample: `1` => `74`, `0` => `1`
- Records with child OIDs: `2`
- Records with supplementary information: `3`

## Local Generated Files

The local run produced:

- `data/sample/sitemap-sample.json`
- `data/sample/records.jsonl`
- `data/sample/records-summary.json`
- `data/sample/crawl-state.json`
- `data/sample/report.json`

These generated files are intentionally ignored by Git because the source terms do not permit publishing a full copied dataset without specific authorization.
If `--save-raw-markdown` is used for local parser debugging, generated sample Markdown files are also ignored and blocked by `npm run guard:publishable`; only `RUN-*.md` receipts should be committed from this directory.
